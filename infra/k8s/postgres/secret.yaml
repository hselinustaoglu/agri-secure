apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: agri-secure
  annotations:
    # WARNING: Replace this with an external secret manager (Sealed Secrets,
    # External Secrets Operator, or Vault) before deploying to production.
type: Opaque
data:
  # changeme base64-encoded — MUST be replaced for production deployments
  POSTGRES_PASSWORD: Y2hhbmdlbWU=
